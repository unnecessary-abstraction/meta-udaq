BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "udaq-layer"
BBFILE_PATTERN_udaq-layer := "^${LAYERDIR}/"
BBFILE_PRIORITY_udaq-layer = "6"

